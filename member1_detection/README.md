## Baseline YOLOv8 Detection

We tested a pretrained YOLOv8n model on a football broadcast video.
The model detects players (class: person) and the ball (class: sports ball).

This baseline confirms that object detection works correctly before
moving to tracking and fine-tuning.